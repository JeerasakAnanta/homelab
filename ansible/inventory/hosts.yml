# Pilot inventory: homepage stack only.
# Default target is `homelab` (direct). Fallback over Tailscale:
#   ansible-playbook playbooks/homepage.yml -e target=homelab-ts
all:
  children:
    homelab_nodes:
      hosts:
        homelab:
          ansible_host: 178.105.115.150
          ansible_user: root
        homelab-ts:
          ansible_host: 100.92.222.34
          ansible_user: deploy
