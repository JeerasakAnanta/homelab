# Host port for the Prefect web UI / API
PREFECT_WEB_PORT=4200

# Postgres storage credentials (used by both the DB container and the Prefect server)
PREFECT_POSTGRES_USER=prefect
PREFECT_POSTGRES_PASSWORD=prefect
PREFECT_POSTGRES_DB=prefect
